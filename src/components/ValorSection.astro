<section class="about-section" id="Acercade" aria-label="Acerca de nosotros">
  <div class="services-inner"><div class="about-grid">

      <div class="about-header">
        <div class="header-label" data-reveal>
          <span class="header-line"></span>
          <p class="eyebrow">ACERCA DE NOSOTROS</p>
        </div>
      </div>

      <div class="about-col">
        <p class="about-phrase" data-reveal>Ingeniería que <span class="text-primary">Energiza</span> tu éxito.</p>
        <p class="about-desc" data-reveal data-delay="1">Somos una empresa profesional de instalaciones electricas de primer nivel.
            Llevamos la energía de media y baja tensión a tus proyectos con precisión técnica y cumplimiento total.</p>
        
        <div class="exp-box" data-reveal data-delay="2">
          <p><strong>+12 años de experiencia</strong>Transformando planos en infraestructura real aprobada por CFE.</p>
        </div>
        
        <a href="#proyectos" class="btn-cta" data-reveal data-delay="3">VER PROYECTOS</a>
      </div>

      <div class="about-col">
        <div class="img-frame main-frame" data-reveal="scale">
          <img src="/Acercade5.jpeg" alt="Ingeniero Powerline" />
        </div>
      </div>

      <div class="about-col">
        <h3 data-reveal>CERO COMPLICACIONES EN TU OBRA</h3>
        <p class="about-desc" data-reveal data-delay="1">Modelo basado en transparencia total: tú pagas por la ingeniería, nosotros garantizamos la aprobación.</p>
        
        <ul class="check-list">
          <li data-reveal data-delay="1"><span>✓</span> Diseño con ingenieria de tus instalaciones.</li>
          <li data-reveal data-delay="2"><span>✓</span> Gestión de trámites ante CFE.</li>
          <li data-reveal data-delay="3"><span>✓</span> Suministro de equipo, herramientas y materiales.</li>
          <li data-reveal data-delay="4"><span>✓</span> Verificación UVIE con la mayor rapidez</li>
        </ul>

        <div class="img-frame mini-frame" data-reveal="scale">
          <img src="/Acercade2.jpeg" alt="Proyecto Powerline" />
        </div>
      </div>

    </div>
  </div>
</section>


<style>
section[id] {
  scroll-margin-top: 60px; /* Permite un margen de vista al hacer clic en el navbar*/
}

	/* Asegura que el contenedor de arriba tenga el mismo ancho y margen que el de abajo */
.services-inner {
    width: var(--content-width);
    margin: 0 auto;
    padding: 0 1rem; /* Un poco de respiro lateral para móviles */
}

/* Fuerza a que todos los headers usen el mismo espaciado vertical */
.services-header {
    display: grid;
    gap: 0.75rem;
}

/* 3. Estilo para los títulos grandes (Propuesta de Valor / Acerca de nosotros) */ 
.services-header h2 {
    font-size: clamp(2rem, 3vw, 3.2rem);
    line-height: 1.05;
    color: var(--color-text);
    margin: 0;
    line-height: 1.05;
    color: var(--color-text);
    font-weight: 800;
    letter-spacing: -0.02em;
}

/* 2. Estilo para el subtítulo pequeño (¿POR QUÉ ELEGIRNOS? / LO QUE HACEMOS) */
.header-label {
    display: flex;
    align-items: center;
    gap: 0.75rem;
	padding-top: 2rem;
}

.header-line {
    width: 2.5rem;
    height: 2px;
    background: var(--color-primary);
    flex-shrink: 0;
}

.eyebrow {
    margin: 0; /* Espacio vertical uniforme */
    color: var(--color-primary);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
}

  /* ─── CONTENEDORES GLOBALES (Mantener) ─── */
  .services-inner {
    width: var(--content-width);
    margin: 0 auto;
    padding: 0 1rem;
  }

  .services-header {
    display: grid;
    gap: 0.75rem;
    margin-bottom: 2rem;
  }

  /* ─── SECCIÓN ACERCA DE (El nuevo diseño profesional) ─── */
  .about-section {
    padding-top: 1rem;
    padding-bottom: 1rem;
    background:#0f1012;; /* Fondo oscuro profundo */
    color: #ffffff;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .about-header {
    grid-column: 1 / -1;
    display: grid;
    gap: 0.75rem;
  }

  .about-col {
    display: flex;
    flex-direction: column;
  }

  .about-phrase {
    font-size: clamp(1.8rem, 2.5vw, 2.2rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .text-primary { 
    color: var(--color-primary, #ff8a00); 
  }

  .about-desc {
    font-size: 1rem;
    line-height: 1.7;
    color: #b0b0b0;
    margin-bottom: 2rem;
  }

  /* Cuadro de Experiencia */
  .exp-box {
    background: #000000;
    border-left: 4px solid var(--color-primary, #ff8a00);
    padding: 2rem;
    margin: 2rem 0;
    border-radius: 0 12px 12px 0;
    box-shadow: 10px 10px 20px rgba(0,0,0,0.2);
  }

  .exp-box strong {
    color: var(--color-primary, #ff8a00);
    display: block;
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
  }

  /* Lista de Checks */
  .check-list {
    list-style: none;
    padding: 0;
    margin-bottom: 2rem;
  }

  .check-list li {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 1.2rem;
    color: #e0e0e0;
  }

  .check-list span {
    color: var(--color-primary, #ff8a00);
    display: flex;
    align-items: center;
  }

  /* Imágenes */
  .img-frame {
    position: relative;
    border-radius: 0.75rem;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  }

  .img-frame img {
    transition: transform 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .img-frame:hover img {
    transform: scale(1.06);
  }

  .main-frame img {
    width: 100%;
    height: 650px;
    object-fit: cover;
  }

  .mini-frame {
    margin-top: 2rem;
  }

  .mini-frame img {
    width: 100%;
    height: 280px;
    object-fit: cover;
  }

  .badge {
    position: absolute;
    bottom: 1.5rem;
    left: 50%;
    transform: translateX(-50%);
    white-space: nowrap;
    background: rgba(0,0,0,0.8);
    backdrop-filter: blur(4px);
    color: #fff;
    font-size: 0.7rem;
    padding: 0.5rem 1rem;
    font-weight: 700;
    border-radius: 4px;
    border: 1px solid rgba(255,255,255,0.1);
  }

  /* Botón */
  a.btn-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary, #ff8a00);
    color: #fff !important;
    padding: 0.55rem 1.1rem;
    font-weight: 700;
    font-size: 1rem;
    letter-spacing: 0.06em;
    text-decoration: none;
    border-radius: 4px;
    transition: background-color 0.2s ease;
    text-transform: uppercase;
  }

  a.btn-cta:hover {
    background: #d9461a;
    color: #fff !important;
  }

  /* Responsive Móvil */
  @media (max-width: 1024px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }
    .main-frame img { height: 340px; }

    .main-frame {
      margin-top: 1.25rem;
    }

    .mini-frame {
      margin-top: 1rem;
      margin-bottom: 1.5rem;
    }

    .header-label {
      padding-top: 1rem;
      justify-content: center;
    }

    .about-header {
      gap: 0;
    }

    .about-col {
      text-align: center;
      align-items: center;
    }

    .check-list li {
      justify-content: center;
    }
  }
</style>