<section class="propuesta-section">
  <div class="services-inner"> <!-- Esta línea es la que da el margen -->
    <header class="services-header">
      <div class="header-label">
        <span class="header-line"></span>
        <p class="eyebrow">¿POR QUÉ ELEGIRNOS?</p>
      </div>
      <h2>Acerca de nosotros</h2>
    </header>
  </div>
</section>

<section class="about-section">
  <div class="services-inner"> <div class="about-grid">
      
      <div class="about-col">
        <p class="about-phrase">Ingeniería que <span class="text-primary">Energiza</span> tu éxito.</p>
        <p class="about-desc">Somos una empresa profesional de instalaciones electricas de primer nivel.
            Llevamos la energía de media y baja tensión a tus proyectos con precisión técnica y cumplimiento total.</p>
        
        <div class="exp-box">
          <p><strong>+12 años de experiencia</strong> transformando planos en infraestructura real aprobada por CFE.</p>
        </div>
        
        <a href="#proyectos" class="btn-cta">MIRA NUESTROS PROYECTOS</a>
      </div>

      <div class="about-col">
        <div class="img-frame main-frame">
          <img src="/MrChao.jpeg" alt="Ingeniero Powerline" />
          <span class="badge">NOM-001-SEDE</span>
        </div>
      </div>

      <div class="about-col">
        <h3>CERO COMPLICACIONES EN TU OBRA</h3>
        <p class="about-desc">Modelo basado en transparencia total: tú pagas por la ingeniería, nosotros garantizamos la aprobación.</p>
        
        <ul class="check-list">
          <li><span>✓</span> Diseño e ingenieria de tus instalaciones.</li>
          <li><span>✓</span> Gestión de trámites ante CFE.</li>
          <li><span>✓</span> Suministro de equipo, herramientas y materiales.</li>
          <li><span>✓</span> Verificación UVIE con la mayor rapidez</li>
        </ul>

        <div class="img-frame mini-frame">
          <img src="/Fracc3.jpeg" alt="Proyecto Powerline" />
        </div>
      </div>

    </div>
  </div>
</section>


<style>
	/* Asegura que el contenedor de arriba tenga el mismo ancho y margen que el de abajo */
.services-inner {
    width: var(--content-width);
    margin: 0 auto;
    padding: 0 1rem; /* Un poco de respiro lateral para móviles */
}

/* Fuerza a que todos los headers usen el mismo espaciado vertical */
.services-header {
    display: grid;
    gap: 0.75rem;
    margin-bottom: left; /* Espacio uniforme debajo del título */
}

/* Estilo unificado para los h2 de las cabeceras */
.services-header h2 {
    margin: 0;
    padding-bottom: 0.5rem; /* Espacio debajo del título */
    font-size: clamp(2rem, 3vw, 3.2rem);
    line-height: 1.05;
    font-weight: 800;
    color: var(--color-text);
}

/* 2. Estilo para el subtítulo pequeño (¿POR QUÉ ELEGIRNOS? / LO QUE HACEMOS) */
.header-label {
    display: flex;
    align-items: center;
    gap: 0.75rem;
	padding-top: 2rem;
}

.header-line {
    width: 2.5rem;
    height: 2px;
    background: var(--color-primary);
    flex-shrink: 0;
}

.eyebrow {
    margin: 0; /* Espacio vertical uniforme */
    color: var(--color-primary);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
}

/* 3. Estilo para los títulos grandes (Propuesta de Valor / Nuestros Servicios) */
.services-header h2 {
    margin: 0;
    font-size: clamp(2rem, 3vw, 3.2rem);
    line-height: 1.05;
    color: var(--color-text);
    font-weight: 800;
    letter-spacing: -0.02em;
	padding-bottom: 2rem;
}

  /* ─── CONTENEDORES GLOBALES (Mantener) ─── */
  .services-inner {
    width: var(--content-width);
    margin: 0 auto;
    padding: 0 1rem;
  }

  .services-header {
    display: grid;
    gap: 0.75rem;
    margin-bottom: 2rem;
  }

  /* ─── SECCIÓN ACERCA DE (El nuevo diseño profesional) ─── */
  .about-section {
    padding-top: 1rem;
    padding-bottom: 1rem;
    background:#0f1012;; /* Fondo oscuro profundo */
    color: #ffffff;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr 1fr;
    gap: 4rem; /* Espacio amplio entre columnas */
    align-items: start;
  }

  .about-phrase {
    font-size: clamp(1.8rem, 2.5vw, 2.2rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .text-primary { 
    color: var(--color-primary, #ff8a00); 
  }

  .about-desc {
    font-size: 1rem;
    line-height: 1.7;
    color: #b0b0b0;
    margin-bottom: 2rem;
  }

  /* Cuadro de Experiencia */
  .exp-box {
    background: #000000;
    border-left: 4px solid var(--color-primary, #ff8a00);
    padding: 2rem;
    margin: 2rem 0;
    border-radius: 0 12px 12px 0;
    box-shadow: 10px 10px 20px rgba(0,0,0,0.2);
  }

  .exp-box strong {
    color: var(--color-primary, #ff8a00);
    display: block;
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
  }

  /* Lista de Checks */
  .check-list {
    list-style: none;
    padding: 0;
    margin-bottom: 2rem;
  }

  .check-list li {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 1.2rem;
    color: #e0e0e0;
  }

  .check-list span {
    color: var(--color-primary, #ff8a00);
    display: flex;
    align-items: center;
  }

  /* Imágenes */
  .img-frame {
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    transition: transform 0.3s ease;
  }

  .img-frame:hover {
    transform: translateY(-5px);
  }

  .main-frame img {
    width: 100%;
    height: 650px; /* Altura para la foto de la canastilla */
    object-fit: cover;
  }

  .mini-frame img {
    width: 100%;
    height: 280px;
    object-fit: cover;
    margin-top: 2rem;
  }

  .badge {
    position: absolute;
    bottom: 1.5rem;
    right: 1.5rem;
    background: rgba(0,0,0,0.8);
    backdrop-filter: blur(4px);
    color: #fff;
    font-size: 0.7rem;
    padding: 0.5rem 1rem;
    font-weight: 700;
    border-radius: 4px;
    border: 1px solid rgba(255,255,255,0.1);
  }

  /* Botón */
  .btn-cta {
    display: inline-block;
    background: var(--color-primary, #ff8a00);
    color: #000;
    padding: 1rem 2.5rem;
    font-weight: 800;
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .btn-cta:hover {
    background: #fff;
    transform: scale(1.05);
  }

  /* Responsive Móvil */
  @media (max-width: 1024px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .main-frame img { height: 450px; }
  }
</style>